# AA Maker 実装計画

## 現在位置

現在は実装前の仕様整理フェーズ。

- `docs/spec.md` に画面構成と主要機能仕様を整理済み。
- Penpot の画面レイアウトを参照済み。
- データモデル詳細ドキュメントは未作成。
- アプリ本体の雛形は未作成。
- フレームワークは未決定。

次に行う作業は、`docs/data-model.md` の作成と MVP 範囲確定。
実装は View 先行で進める。

## フェーズ一覧

### Phase 0: 仕様整理

目的: 実装前に UI、グリッド、色、レイヤー、パレット、スタンプの仕様を読める状態にする。

状態: 完了

完了条件:

- [x] 80x25 グリッド仕様を定義する
- [x] 半角/全角のセル消費仕様を定義する
- [x] `キャンバスBGC` / `BGC` / `FGC` を定義する
- [x] セル状態を定義する
- [x] 左ツールボックスを定義する
- [x] 右サイドバーを定義する
- [x] キャラパレットセットを定義する
- [x] スタンプパレットを定義する
- [x] レイヤー仕様を定義する
- [x] 未確定事項を整理する

### Phase 1: データモデル設計ドキュメント

目的: 実装前に、編集データの持ち方を `docs/data-model.md` にまとめる。

状態: 次に着手

成果物:

- `docs/data-model.md`

設計対象:

- `Cell`
- `Layer`
- `Document`
- `Selection`
- `ToolState`
- `CharPalette`
- `Stamp`
- `ColorScheme`

完了条件:

- [ ] `Cell` の状態をドキュメント化する
- [ ] 未配置セルと `U+00A0 NBSP` の扱いをドキュメント化する
- [ ] レイヤー合成ルールをドキュメント化する
- [ ] `キャンバスBGC` とセル `BGC` の分離をドキュメント化する
- [ ] スタンプの白黒/カラー種別をドキュメント化する
- [ ] 範囲選択と配置範囲をドキュメント化する
- [ ] View 実装時に使う仮データ構造を決める

### Phase 2: MVP 範囲確定

目的: 最初に動くアプリとして実装する範囲を固定する。

状態: 未着手

推奨 MVP:

- 80x25 グリッド表示
- レイヤー 1 枚
- ペン
- 消しゴム
- スポイト
- 通常キャラパレット
- 選択中文字表示
- `キャンバスBGC` の白/黒切り替え

完了条件:

- [ ] MVP に含める機能を確定する
- [ ] MVP から外す機能を明記する
- [ ] 実装順を確定する

### Phase 3: アプリ雛形作成

目的: `apps/aa-maker` に SPA の開発環境を作る。

状態: 未着手

候補:

- Vite
- React
- TypeScript

完了条件:

- [ ] `apps/aa-maker` でアプリが起動できる
- [ ] 開発サーバーを起動できる
- [ ] 基本ディレクトリ構成を作る
- [ ] 画面に空の AA Maker UI を表示できる

### Phase 4: UI 骨格実装

目的: Penpot 参照に沿って、編集ロジックより先に View を完成させる。

状態: 未着手

実装対象:

- 上部メニューバー
- 左ツールボックス
- 中央編集領域
- 右サイドバー
- 情報表示
- キャラパレット
- レイヤー欄
- スタンプパレットの見た目
- SVG アイコン

完了条件:

- [ ] 画面構成が Penpot 参照に近い
- [ ] 左右サイドバーと中央グリッドが崩れない
- [ ] ツールボックスの SVG アイコン方針を反映する
- [ ] 選択中ツールを左縦線で表示する
- [ ] 中央グリッドをなめらかにズームできる View を用意する
- [ ] 仮データで各 UI 状態を表示できる

### Phase 5: データモデル実装

目的: `docs/data-model.md` に基づいて、View に接続するデータ構造を実装する。

状態: 未着手

実装対象:

- `Cell`
- `Layer`
- `Document`
- `Selection`
- `ToolState`
- `CharPalette`
- `Stamp`
- `ColorScheme`

完了条件:

- [ ] `Cell` の状態を TypeScript 型として定義できる
- [ ] 未配置セルと `U+00A0 NBSP` の扱いを型で表現できる
- [ ] `キャンバスBGC` とセル `BGC` を分離できる
- [ ] スタンプの白黒/カラー種別を表現できる
- [ ] 範囲選択と配置範囲を表現できる

### Phase 6: グリッド編集ロジック実装

目的: UI から独立した編集ロジックを作る。

状態: 未着手

実装対象:

- 文字幅判定
- セル配置
- セル削除
- スポイト
- レイヤー合成
- `キャンバスBGC` 表示

完了条件:

- [ ] 半角文字を 1 セルで配置できる
- [ ] 全角文字を 2 セルで配置できる
- [ ] 全角文字の片側削除で文字全体を削除できる
- [ ] `BGC:0` で `キャンバスBGC` を表示できる
- [ ] レイヤー合成で空白セルが透過する

### Phase 7: パレット実装

目的: 選択中文字を UI から変更できるようにする。

状態: 未着手

実装対象:

- 通常キャラパレット
- ヒストリーパレット
- キーボードインプット
- Unicode 文字表

完了条件:

- [ ] キャラパレットクリックで選択中文字が変わる
- [ ] スポイトでヒストリーが更新される
- [ ] 同じキャラが連続でヒストリー追加されない
- [ ] Unicode 文字表が仮想スクロールで表示される

### Phase 8: レイヤー実装

目的: 複数レイヤーの編集と合成を実装する。

状態: 未着手

完了条件:

- [ ] レイヤーを追加できる
- [ ] レイヤーを削除できる
- [ ] 最低 1 レイヤーを維持する
- [ ] 表示/非表示を切り替えられる
- [ ] ロックを切り替えられる
- [ ] ドラッグで順番を入れ替えられる
- [ ] 下から上に合成表示できる

### Phase 9: スタンプ実装

目的: スタンプ選択、プレビュー、配置を実装する。

状態: 未着手

完了条件:

- [ ] スタンプセットを選択できる
- [ ] スタンプリストを表示できる
- [ ] 半透明プレビューを表示できる
- [ ] クリックで配置できる
- [ ] グリッド外にはみ出す場合は収まる範囲だけ配置できる
- [ ] 配置後に配置範囲を選択状態にできる

### Phase 10: 保存・読み込み・エクスポート

目的: 作成した AA を外部に出し入れできるようにする。

状態: 未着手

完了条件:

- [ ] 保存形式を決める
- [ ] 読み込み形式を決める
- [ ] エクスポート形式を決める
- [ ] 行末スペースの扱いを決める
- [ ] 色情報を保存するか決める

## 現在の次タスク

1. `docs/data-model.md` を作る
2. MVP 範囲を確定する
3. アプリ雛形を作成する
4. View 実装に着手する

## 要確認・未確定

- 全角文字が行末に収まらない場合の扱い
- 既存文字に重ねて配置する場合の上書き仕様
- 移動先がグリッド外にはみ出す場合の扱い
- ペンの連続描画
- 消しゴムのドラッグ削除
- テキスト入力時の折り返し・改行・範囲外入力
- 空白セルをスポイトした場合の扱い
- 反転系アイコン 2 種の正式仕様
- キーボードショートカット
- Undo / Redo

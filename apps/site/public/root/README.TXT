Elegg Retro-Tube

This site runs inside a 512x256 ANSI terminal buffer.
The visible screen is a WebGL CRT shader over a low-resolution console.

Try:
  ls -la /home/guest
  cat /home/guest/ABOUT.TXT
  cd /var/log
  cat SYNC.LOG

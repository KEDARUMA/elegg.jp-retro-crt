# KEDARUMA

## Verification

- This project is not a React Native project.
- Do not run `rn-verify` in this project.
- When using `commit-msg`, skip the `rn-verify` precheck.
- Do not run `build` after normal code changes. The dev server uses HMR, so prefer browser reload or server status checks.
- Run `build` only when starting a server, when explicitly requested, or when a production build check is the task.
